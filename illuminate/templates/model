import { model, Schema, Document, InferSchemaType } from "mongoose";
import Timestamps, { ITimestamps } from "app/traits/Timestamps";

const {{name}}Schema = new Schema({
  //
});

{{name}}Schema.plugin(Timestamps);

export type IPlugins = ITimestamps;
export interface I{{name}} extends Document, InferSchemaType<typeof {{name}}Schema>, IPlugins {};

export default model<I{{name}}>("{{name}}", {{name}}Schema);