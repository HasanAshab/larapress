import { model, Schema, Model, Document, InferSchemaType } from "mongoose";
import Timestamps, { ITimestamps } from "app/traits/Timestamps";

const {{name}}Schema = new Schema({
  //
});

{{name}}Schema.plugin(Timestamps);

type IPlugin = ITimestamps;
export type I{{name}} = Document & InferSchemaType<typeof {{name}}Schema> & IPlugin["instance"];
type {{name}}Model = Model<I{{name}}> & IPlugin["statics"];
export default model<I{{name}}, {{name}}Model>("{{name}}", {{name}}Schema);