import { model, Schema, Model, Document, InferSchemaType } from "mongoose";
import HasFactory, { HasFactoryModel } from "app/plugins/HasFactory";

const {{name}}Schema = new Schema(
{
  //
},
{ timestamps: true }
);

{{name}}Schema.plugin(HasFactory);


export interface I{{name}} extends Document, InferSchemaType<typeof {{name}}Schema> {};
interface {{name}}Model extends Model<I{{name}}>, HasFactoryModel {};
export default model<I{{name}}, {{name}}Model>("{{name}}", {{name}}Schema);