import { middleware } from "helpers";
import express, { Router } from "express";

const router: Router = express.Router();
const {{name}}Controller = controller("{{name}}Controller");


// Endpoints for {{name}}
router.get("/", {{name}}Controller.index);


export default router;
