const { body } = require('express-validator');
const User = model('User');

class {{name}} {
  static index = {
    validator:[
      //
    ],
    
    method: (req, res)=>{
      //
    }
  }
}

module.exports = {{name}};