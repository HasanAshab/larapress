const CatchAllMethodErrors = require(base("utils/CatchAllMethodErrors"));
const User = model('User');

class {{name}} {
  static index = (req, res) => {
    //
  }
}

CatchAllMethodErrors.wrapMethods({{name}});
module.exports = {{name}};