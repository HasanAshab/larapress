const faker = require("faker");
const Factory = require(base('app/factories/Factory'));

class {{name}} extends Factory {
  definition = () => {
    return {
      //
    };
  };
}

module.exports = {{name}};
