import Job from "~/core/abstract/Job";

export default class {{name}} extends Job {
  //concurrency = 1;
  
  constructor(private data: object = {}) {
    super();
    this.data = data;
  }

  async handle(){
    //
  }
}