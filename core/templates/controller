import { controller } from "~/core/decorators/class";
import { Request, Response } from "express";

@controller
export default class {{name}}Controller {
  async index(req: Request, res: Response) {
    res.message("{{name}}Controller works!");
  }
}