import Factory from "~/core/interfaces/Factory";
import { faker } from "@faker-js/faker";

export default class {{name}}Factory implements Factory {
  definition() {
    return {
      //
    };
  };
}