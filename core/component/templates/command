import Command from "~/core/abstract/Command";

export default class {{name}} extends Command {
  static signature = "command signature";
  static description = "command description";

  async handle(){
    this.success("{{name}} command works!");
  }
}