import { middleware } from "~/core/utils";
import express, { Router } from "express";

const router: Router = express.Router();


router.get("/", (req, res) => {
  res.message("Hello from Larapress!");
});


export default router;
