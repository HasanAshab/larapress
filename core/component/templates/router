import { middleware, controller } from "~/core/utils";
import express, { Router } from "express";

const router: Router = express.Router();
const Controller = controller("Controller");

// Endpoints for {{name}}

router.get("/", Controller.index);


export default router;
