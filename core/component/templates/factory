import Factory from "~/core/database/Factory";
import { faker } from "@faker-js/faker";
import { I{{name}}, {{name}}Document } from "~/app/models/{{name}}";


export default class {{name}}Factory extends Factory<I{{name}}, {{name}}Document> {
  /**
   * Return initial state of the {{name}}
  */
  definition() {
    return {
      //
    };
  };
}