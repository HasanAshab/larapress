import Joi from "joi"; 
import mongoose from "mongoose";
/*
const isValidObjectId = mongoose.Types.ObjectId.isValid('your-string-here');
console.log(isValidObjectId); // true or false
static schema = {
    urlencoded: {
      target: 'params',
      rules: Joi.object({
        id: Joi.string().required(),
      })
    },
}

*/