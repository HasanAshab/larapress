require('dotenv').config();
require('./main/register').registerHelpers();
const args = process.argv;
const Artisan = require(base('utils/Artisan'));

try {
  Artisan.getCommand(args.splice(2))();
} catch (err) {
  console.log(err);
  process.exit(1);
}
